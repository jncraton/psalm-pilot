<!doctype html>
<html lang="en-US">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Song pilot" />
    <link rel="stylesheet" type="text/css" href="style.css" />
    <link rel="icon" type="image/svg+xml" href="Logo/favicon.svg">
    <title>Psalm Pilot</title>
  </head>
  <body>
    <header>
      <img id="h1" src="Logo/1000x400xforWebBrowserHTML.svg" alt="Psalm Pilot Logo">
    </header>
    <main>
      <table>
        <thead>
          <tr>
            <th>Hymn Title</th>
            <th>Hymn Author</th>
          </tr>
        </thead>
        <tbody>
          {% for hymn in hymns %}
          <tr>
            <td>{{ hymn.title }}</td>
            <td>{{ hymn.authors or ''}}</td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </main>
  </body>
</html>
