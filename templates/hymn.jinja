{% extends 'base.jinja' %}

{% set root_dir, page_title = '../', hymn.title %}

{% block main %}
  <div>
    <h2>{{ hymn.title }}</h2>
    <blockquote>{{ hymn.text }}</blockquote>
  </div>
{% endblock %}