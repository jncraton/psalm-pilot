{% extends 'base.jinja' %}
{% block header %}{% endblock header %}
{% set root_dir, page_title = '../', hymn.title %}

{% block main %}
  <div>
    <h2>{{ hymn.title }}</h2>
    <p>{{ hymn.text }}</p>
  </div>
{% endblock %}